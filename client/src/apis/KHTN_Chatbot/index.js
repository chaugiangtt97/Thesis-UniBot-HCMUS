import { determine_collection } from "./determine_collection";
import { generate } from "./generate"
import { search } from "./search"
import { extract_meta} from "./extract_meta"

export const useKHTN_Chatbot = {
    determine_collection,
    generate,
    search,
    extract_meta
}